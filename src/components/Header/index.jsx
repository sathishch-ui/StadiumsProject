import React from "react";

const Header = () => {
  return (
    <header className="flex flex-end text-xl font-semibold items-center justify-between p-4 bg-gray-800 text-white">
      Logout
    </header>
  );
};

export default Header;
